node_modules
dist-ssr
.DS_Store
*.local
.idea
out/
resume/out/

# dist/ is intentionally tracked: Cloudflare serves the prerendered output
# directly, since its build image cannot run puppeteer's Chromium.
