# Vincent Portfolio

A fast, static portfolio site built with **Vite + React + TypeScript + Tailwind CSS**,
routed with **React Router**.

## To run my site locally

```bash
npm install
npm run dev
```

## To build locally

```bash
npm run build
npm run preview
```
